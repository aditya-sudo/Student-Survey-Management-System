// Enum representing valid options for the source of interest field (e.g., friends, television, etc.).
// Ensures input consistency for the "sourceOfInterest" column in the database.

package com.example.demo.model;

public enum SourceOfInterest {
    friends,television,internet,relatives 
 }